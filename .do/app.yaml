# DigitalOcean App Platform spec.
# Create -> Apps -> your GitHub repo -> "Edit Plan" -> import this, or just make
# sure the component type is WORKER (not Web Service).
#
# A long-polling bot never binds a port. A Web Service health-checks the port,
# finds nothing, and restarts the container forever. Worker has no health check.
name: telegram-video-broadcaster

workers:
  - name: broadcaster
    github:
      # owner/repo — App Platform rejects a full https:// URL here.
      repo: 250262-coder/telegram-video-broadcaster
      branch: main
      deploy_on_push: true

    dockerfile_path: Dockerfile
    instance_count: 1
    instance_size_slug: apps-s-1vcpu-0.5gb

    # BOT_TOKEN, DATABASE_URL, VAULT_CHAT_ID and ADMIN_IDS are deliberately NOT
    # listed here. Set them as App-Level environment variables in the dashboard
    # instead — that keeps them off GitHub, and a component-level key with no
    # value would override the app-level one with an empty string.
    envs:
      - key: INTERVAL_HOURS
        scope: RUN_TIME
        value: "4"
      - key: DELAY_BETWEEN_GROUPS
        scope: RUN_TIME
        value: "2.0"
      - key: CAPTION_SUFFIX
        scope: RUN_TIME
        value: ""
